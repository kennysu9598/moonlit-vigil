# 20260917T031000 vfx18-title

- **Author / agent**: glm-general（GLM-5.3-Flash 施工席 worker；主调度员按 Leader 卡派送，四合一：接线 + dev-log + 集成验收 + 录屏）
- **Date**: 2026-09-17T03:10:00-04:00
- **Reason**: v0.6.0 波次1 收尾——把 18 招特效系统的落地记录补进 dev-log，并把未接线的 `title_theme.ogg` 接到标题段音乐，完成集成验收与实录证据。

## 一、18 招特效系统（现状概括，依据 `archives/20260917-vfx-18/` 前置版本 diff 与 `scripts/skill_vfx.gd`）

- **SPECS 表**（`skill_vfx.gd` L22-41）：18 招 = 6 单位 × 3 技能，键 `u<unit>s<slot>`。每招写死主色 `c`、高光 `hi`、预警形 `tele`（sigil / ground_sigils / double_ring / fan / ring_collapse / ring_breathe）、环半径 `tr`、全屏闪 `k`、白核 `white`、粒子配方 `burst`、暗角 `vig`，T3 终结技另有 `arena` 场面（ring_charge / crows / mist）。参数按 R7 设计表冻结。
- **分档时间线**：前摇 `WINDUP` 0.06/0.09/0.14 s，峰值 `PEAKS` 0.18/0.40/1.55 s（T1/T2/T3）；终结技定格 `freeze_for` 0.08–0.10 s。
- **护盾双通道**：状态通道 `update_shields()` 持续描边（`shield_outline.gdshader` 轮廓辉光，alpha 随 `SHIELD_MAX=60` 归一；`actor.gd` 侧 `shield_gained`/`shield_broken` 信号 + 0.25 s tween 填充）＋事件通道 `shield_gain()` / `shield_break_burst()` 即时粒子。
- **Boss 预警三合一**：u5s1 `begin_boss_charge()` 蓄力法阵 ＋ `_draw_charge_bar()` 屏上蓄力条 ＋ u5s2 `release_boss_charge()` / `interrupt_flash()` 释放与打断闪光。
- **动作增强四点**（`actor.gd` 相对 vfx-18 归档版 diff）：分档前摇 `WINDUP`、受击位移 `PUSH` 5/15/24、冲刺残影（dash ghosts）、阵亡倒地定妆（persistent fallen death pose，低 alpha `DEATH_FADE`，战斗结束前不消失）。
- **main.gd 接线**：`vfx.play_skill()` / `on_impact()` / `reset()` 三口接入施放→命中→重开链路；tier 贯通 `strike()` 与 `fx.play()`；非终结 fire / raven_fire / charge 附带环境染色 0.5。

## 二、title_theme 接线

- `scripts/audio_manager.gd` MUSIC 流表 `ambience` 键：`moon_ambience_loop.wav` → `title_theme.ogg`（标题段即播新曲；`_make_looping()` 对 OggVorbis 置 loop=true 的逻辑原样沿用；battle / win / lose 三键不动）。
- 旧流 `moon_ambience_loop.wav` 连同 `.import` 复制归档至 `assets/audio/_archive-20260917/`，原文件未删。
- 改前快照 `audio_manager.gd` / `test_audio.gd` 存 `archives/20260917-title-theme/`。
- `tests/test_audio.gd` 新增 1 条断言：ambience 键必须指向 `title_theme.ogg`。
- 登记核对：`title_theme.ogg` 已在 `ASSET_LICENSES.md`（ACE-Step 1.5 AI-music 产线，项目原创）与 `audio-manifest.json` 有条目；原条目注「尚未接入」——本次接线后该状态解除。

## Changed files

- `scripts/audio_manager.gd`（MUSIC 表 1 行改指）
- `tests/test_audio.gd`（+1 断言）
- `archives/20260917-title-theme/audio_manager.gd`、`archives/20260917-title-theme/test_audio.gd`（改前快照）
- `assets/audio/_archive-20260917/moon_ambience_loop.wav` + `.import`（旧流归档）
- `docs/dev-log/20260917T031000_vfx18-title.md`（本文件）

## Checks (evidence)

- 引擎：winget `Godot_v4.7.2-stable_win64.exe`（4.7.2.stable.official.ed1daf0bf；路径取自 `.godot/editor/project_metadata.cfg` 记录）。
- `--headless --editor --import --quit` EXIT=0。stderr 仅存量的归档拷贝 UID duplicate WARNING（voice-v1 等，前人遗留，非本次引入）。
- 六测试全绿（EXIT=0，SCRIPT ERROR=0）：
  - combat `failures=0 seeds=100 wins=100 actions_mean=33.99 range=29..42`
  - auto `failures=0 seeds=100 wins=100 mean_actions=30.64`
  - art_fx `ART_FX_TEST PASS local_motion/freeze/shared_mesh/no_fold/reset/expiry`
  - audio `AUDIO_TEST failures=0`（含新增 title_theme 断言）
  - voice `VOICE_TEST failures=0`
  - vfx `VFX_TEST failures=0 constructs=18 shield=on charge=on`
- boot 冒烟 `--headless --quit-after 30` EXIT=0，SCRIPT ERROR=0（仅引擎常规 "2 resources still in use at exit" 提示）。
- 踩坑记录：用 `dist/*/MoonlitVigil.exe`（打包改名副本）以 `--path` 跑 import 会刷 `Cannot open file 'res://scripts/*.gdc'` 且异常退出；一律改用 winget 正主 exe。

## Unverified / remaining

- 真听感（title_theme 实际响度/循环衔接）未人耳验收；录屏为纯视频（gdigrab 不采声，本机无 virtual-audio-capturer，未授权安装），音频验证仍靠测试断言 + manifest 既有 ffprobe/ebur128 记录。
- 录屏产物与抽帧证据见 `C:\Users\Desktop\AppData\Local\Temp\opencode\mv-v06-record\`（见当次回执）。
- 下一波次项（Boss 战二阶段表现、结算画面打磨等）不在本卡范围。
