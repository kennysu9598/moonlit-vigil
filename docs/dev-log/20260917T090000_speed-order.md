# Speed-driven turn order + enrage speed

Date: 2026-09-17. Author: worker (glm-general, dispatched by main scheduler). File ownership: scripts/combat.gd + tests/test_combat.gd only.

Change:
- combat.gd: fixed `_order=[0,3,2,4,1,5]` replaced by `_turn_order`, derived once at start() from speeds (descending, index-stable tiebreak via explicit comparator). advance() iterates the derived order. Boss (id 5) now gains +20 speed on enrage (50→70, not reverted) and the order is rebuilt at that moment; cursor re-anchored to current actor so per-round invariants hold.
- tests/test_combat.gd: +2 assertions — "speed order derived" ([0,3,2,4,1,5]) and "enrage order shift" (speed 70, order [0,3,2,4,5,1]). Win-rate thresholds unchanged (boss strength change happens post-half-HP; 100-seed wins stay 100).
- Archive: archives/20260917-speed-order/combat.gd (pre-change copy).
- auto_policy.gd: read-only check — it consumes only combat.current_id/units, no order fields; compatible.

Reason: make speeds actually drive action order (data-driven) and give enrage a tactical speed consequence.

Verification (Godot 4.7.2 headless, console exe, engine error scan clean):
- test_combat: failures=0 seeds=100 wins=100 actions_mean=33.99 range=29..42, EXIT=0
- test_auto: failures=0 seeds=100 wins=100 mean_actions=30.64, EXIT=0
- test_art_fx: PASS, EXIT=0 (regression)

Derived order check: speeds [100,60,80,90,70,50] → 100(0)>90(3)>80(2)>70(4)>60(1)>50(5) = [0,3,2,4,1,5], identical to the old hardcoded order (behavior-compatible refactor). After enrage: [0,3,2,4,5,1].

Untested / follow-up: main.gd HUD (queue_label L441-443, turn_icons L448) keeps its own hardcoded [0,3,2,4,1,5] display and does not read combat order fields — logic is unaffected, but the HUD label will not reflect the post-enrage order until main.gd is adapted (out of scope for this task, owner: main.gd writer). Windowed visual pass not performed (logic-only change).
