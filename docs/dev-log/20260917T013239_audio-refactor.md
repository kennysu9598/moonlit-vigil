# 20260917T013239 · 音频系统重构（AudioMgr autoload + 池化 + 总线 + crossfade + 三曲制）

- 身份：施工席 worker（glm-general），受派工卡《音频系统重构》执行；本卡唯一写手范围=main.gd。
- 时间：2026-09-17 01:32（本地）

## 改动文件

| 文件 | 改动 |
| --- | --- |
| scripts/audio_manager.gd | 新建（100 行）。autoload `AudioMgr`：代码内建 Music/SFX 总线（挂 Master，不依赖 default_bus_layout.tres）；2 通道音乐 crossfade（0.8s tween 出+入，同 key 不重播）；8 路 SFX round-robin 池（pitch 抖动 ±0.06）；`set_muted` 只动两总线 mute；ogg→`stream.loop=true`、WAV→按 data 帧数算 loop_end（删除 loop_end=529200 魔数）；全部资源带 `ResourceLoader.exists` 兜底。 |
| scripts/main.gd | 归档后接线（582→565 行）：删 music/sfx 字段与三段旧实现（旧 L30-31、L200-210、L302-307、L526-535）；`_ready`→play_music("ambience")；`_start_battle`→play_music("battle")；`_finish` 结算面板→按胜负 play_music("win"/"lose")；`_cast` perform 成功后按 kind 播 cast_fire/heal/shield/stun（命中音保持 impact_*，未动）；`_show_events` 的 charge/interrupt 处接 charge_start/charge_interrupt；`_toggle_mute`→AudioMgr.set_muted（废弃 volume_db=-80 与 stream_paused 双机制）；`_sound` 全部调用点原样保留、内部改走 AudioMgr.play_sfx。结算文案「长夜已过/灯火暂熄」原样未动。 |
| project.godot | 新增 `[autoload]` 段：`AudioMgr="*res://scripts/audio_manager.gd"`。 |
| tests/test_audio.gd | 新建（SceneTree 脚本）：断言 8 池+2 通道建成且挂对总线、两总线存在、缺文件 play_sfx 静默跳过、存在文件入池且 pitch 有界、play_music 同 key 不重启（music_stream_assigns 计数）、缺文件音乐跳过、换 key crossfade、set_muted 两总线 mute 状态正确。含 10s 超时保险。 |
| archives/20260917-audio-refactor/main.gd | 改动前 main.gd 归档（MD5 与改前一致：8E9FC7B132EA3BDCB98F47C20950C137）；archives/ 加 .gdignore。 |
| docs/dev-log/20260917T013239_audio-refactor.md | 本条。 |

## 原因

派工卡：音频旧实现为单实例播放器（音效会互相打断）、双静音机制易漏、BGM 硬切、魔数 loop_end。重构为 autoload 统一管理。

## 检查与证据（Godot 4.7.2.stable.official.ed1daf0bf，全部本机实测）

- `godot --headless --path . --import --quit`：完成（DONE reimport）；错误仅来自并行资源卡暂存区 `assets/_incoming-audio-20260…/` 内的 macOS `._*.wav` AppleDouble 垃圾（82 字节、RIFF 缺头），与本卡改动无关，assets\ 本卡不动。
- `res://tests/test_audio.gd` → `AUDIO_TEST failures=0`
- `res://tests/test_combat.gd` → `COMBAT_TEST failures=0 seeds=100 wins=100 actions_mean=33.99 range=29..42`
- `res://tests/test_auto.gd` → `AUTO_TEST failures=0 seeds=100 wins=100 mean_actions=30.64 range=28..36`
- `res://tests/test_art_fx.gd` → `ART_FX_TEST PASS local_motion/freeze/shared_mesh/no_fold/reset/expiry; aesthetics NOT evaluated`
- 启动冒烟：`godot --headless --path . --quit-after 30` 零 SCRIPT ERROR / Parse Error（autoload 注册 + main.gd 接线实际加载通过）。

## 未验证 / 遗留

- 真实听感（音量配比、crossfade 曲线、pitch 抖动手感、三曲衔接）未做人耳验收——需窗口模式实际游玩，归主调度员/独立审查门。
- `assets/_incoming-audio-20260…/` 暂存目录及其 `._*` 文件导致 import 日志报错，建议资源卡收尾时清理或加 .gdignore（本卡不动 assets\）。
- 未改 docs/STATUS.md 与 CHANGELOG.md（多卡并行，避免冲突，留调度员合并）。
- 音频资产清单/许可由并行资源卡负责，本卡未核（assets/licenses/audio-manifest* 已存在）。
